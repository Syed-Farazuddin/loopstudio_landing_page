/* eslint-disable */

import { Fragment } from "react";
import Header from "../Header/Header";

export default function Layout({ children }) {
  return (
    <Fragment>
      {/* <Header />
      {children} */}
    </Fragment>
  );
}
